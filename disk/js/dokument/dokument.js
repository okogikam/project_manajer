class dokument extends mainScript {
    constructor(mc) {
        super();
        this.project = mc.project;
        this.task = mc.task;
        this.log = mc.log;
        this.userList = mc.userList;
    }
}